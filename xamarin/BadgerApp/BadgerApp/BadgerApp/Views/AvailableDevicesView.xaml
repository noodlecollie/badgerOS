<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BadgerApp.ViewModels"
             x:Class="BadgerApp.Views.AvailableDevicesView"
             x:Name="availableDevicesPage"
             Title="Available Devices"
             Padding="10">
    <ContentPage.Padding>
        <OnPlatform x:TypeArguments="Thickness" iOS="0, 20, 0, 0"/>
    </ContentPage.Padding>
    <ContentPage.BindingContext>
        <vm:AvailableDevicesViewModel/>
    </ContentPage.BindingContext>
    <Frame>
        <StackLayout Orientation="Horizontal" Spacing="0" HorizontalOptions="Center" VerticalOptions="Center">
            <Label Text="{Binding DateTime}" />
        </StackLayout>
    </Frame>
</ContentPage>